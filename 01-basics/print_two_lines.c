#include <stdio.h>
/**
 * main - Entry point;
 * takes no arguments;
 *
 * prints the name in qoutes using the escape character;
 * Return: - has no return value;
 */
int main(void)
{
	printf("Hello\nCall me Felix: \"Felix\"\n");
	return (0);
}
