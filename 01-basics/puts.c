#include <stdio.h>

/**
 * main - Programs entry point;
 * takes no parameters;
 *
 * Return: - Has no return type;
*/
int main(void)
{
	/* Using puts in place of printf */
	puts("Thrilled to use puts in place of printf");
	return (0);
}
